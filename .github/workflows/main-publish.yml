name: Publish Image

on:
  push:
    branches:
      - main

jobs:
  publish-image:
    name: Publish Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Prepare reports directory
        run: mkdir -p reports

      - name: Build Docker image
        id: docker_build
        run: docker build -t ghcr.io/zinedinealvarez/secure-kube-ops:${{ github.sha }} .

      - name: Login to GitHub Container Registry
        id: ghcr_login
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image to GHCR
        id: ghcr_push
        run: docker push ghcr.io/zinedinealvarez/secure-kube-ops:${{ github.sha }}

      - name: Record published image evidence
        if: always()
        run: |
          cat > reports/published-image-metadata.json <<EOF
          {
            "workflow": "${{ github.workflow }}",
            "run_id": "${{ github.run_id }}",
            "commit": "${{ github.sha }}",
            "image": "ghcr.io/zinedinealvarez/secure-kube-ops:${{ github.sha }}",
            "build_outcome": "${{ steps.docker_build.outcome }}",
            "login_outcome": "${{ steps.ghcr_login.outcome }}",
            "push_outcome": "${{ steps.ghcr_push.outcome }}"
          }
          EOF

      - name: Write Publish Image summary
        if: always()
        run: |
          {
            echo "## Publish Image evidence"
            echo ""
            echo "| Control | Result | Evidence |"
            echo "| --- | --- | --- |"
            echo "| Docker build | ${{ steps.docker_build.outcome }} | published-image-metadata.json |"
            echo "| GHCR login | ${{ steps.ghcr_login.outcome }} | published-image-metadata.json |"
            echo "| GHCR push | ${{ steps.ghcr_push.outcome }} | published-image-metadata.json |"
            echo ""
            echo "Image: \`ghcr.io/zinedinealvarez/secure-kube-ops:${{ github.sha }}\`"
            echo "Commit: \`${{ github.sha }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Publish Image reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: securekubeops-publish-image-${{ github.run_id }}-${{ github.sha }}
          path: reports/
          retention-days: 90
          if-no-files-found: warn
